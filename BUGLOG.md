# 🦊 پروندهٔ باگ‌ها (درجه ۴ — هرگز پاک نشود)
| # | تاریخ | باگ | ریشه | قانون پیشگیری |
|---|---|---|---|---|
| ۱ | ۰۹-۱۹ | آلودگی متقاطع D1 (هلند active آلمان را می‌نوشت) | ورکرهای چندکشوری با id='de' | ورکر نو = SRV_ID اختصاصی + سطر active_srv خودش |
| ۲ | ۰۹-۱۹ | پیچ gist خطِ نود دیگر را خراب می‌کرد | PATCH بدون تطبیق خط | line-aware با uuid/host یکتا |
| ۳ | ۰۹-۱۹ | ترمیم آلمان کرش می‌کرد | de-pool.json اصلاً در ریپو نبود | هر نود pool_file موجود+معتبر قبل از فعال‌شدن |
| ۴ | ۰۹-۱۹ | selfheal محلی foxy-de3 را DELETE کرد | false-negative (نبود vless_bridge.py محلی) | هیچ حذفی بدون گارد پیش‌نیاز |
| ۵ | ۰۹-۱۹ | لینک‌های gist چند دقیقه خراب | بازنویسی رشته علامت @ را انداخت | بازسازی کامل خط از الگو، نه جابجایی |
| ۶ | ۰۹-۱۹ | ۴ شکست پیاپی nl-hunt (CF 10021) | filename پارت ≠ main_module | filename = main_module همیشه |
| ۷ | ۰۹-۱۹ | nl-hunt بعد از کارِ سالم «failure» | git pathspec nl-pool.json | `git add -A` tolerant |
| ۸ | ۰۹-۱۹ | watch کرش در اجرای اول | AbortSignal.timeout در workerd نیست | فقط الگوهای تست‌شدهٔ workerd |
| ۹ | ۰۹-۱۹ | فچ worker→workers.dev = 404 (1042) | CF فچ هم‌اکانت ممنوع | چک داخلی فقط با service binding |
| ۱۰ | ۰۹-۱۹ | کرون */5 اصلاً ثبت نشده بود | metadata `schedules` نادیده | ثبت با endpoint /schedules + GET تأیید |
| ۱۱ | ۰۹-۱۹ | SyntaxError JS در دیپلوی | پایتونیسم l[8:]/splitlines در کدتولیدشده | پارس JS الزامی قبل از دیپلوی |
| ۱۲ | ۰۹-۱۹ | «۷ گره در ساب» (هشدار کاذب ممیزی) | شمردن ۴ خط اطلاعاتی uuid-صفر | مقایسه فقط خطوط دامنهٔ واقعی |
| ۱۳ | ۰۹-۱۹ | حلقهٔ بازسازی host-کهنه | ریپو host قدیمی داشت بعد از ترمیم | host+uuid فقط از gist + sync پس از هر ترمیم |
| ۱۴ | ۰۹-۱۹ | فرض capacity.py در ریپو (404) | منطق inline داخل yml است | قبل از فرض، درخواست واقعی |
| ۱۵ | ۰۹-۱۹ | نگهبان از کشور نو بی‌خبر می‌ماند | شکارچی نود اضافه می‌کرد، binding نه | هر افزودن نود = بازسیم نگهبان |
| ۱۶ | ۰۹-۱۹ | ریشهٔ ورکرها دوباره ۲۰۰ شد (بنر فاکس برگشت) | پچ ریشه هرگز push نشد؛ بازسازی‌ها از ریپوی عقب بودند | پچ امنیتی = push فوری ریپو؛ بازمبنا فقط از ریپوی سینک‌شده |
| ۱۷ | ۰۹-۱۹ | sync_nodes_file با 422 شکست خورد | PUT آپدیت بدون sha (فقط create) | هر PUT contents = اول GET sha |
| ۱۸ | ۰۹-۱۹ | نگهبان ژاپن را چک نمی‌کرد | پچ نیمه‌کاره: MAP ۱۱کشوری شد، NODES ثابت ماند | هر گسترش شناسه = همهٔ نقشه‌های مرتبط در یک کامیت + جستجوی مراجع |
| ۱۹ | ۰۹-۱۹ | دو بار «خراب» پنداشتن ورکر تازه‌دپلوی | تأخیر پخش workers.dev ~۳۰-۶۰ثانیه | بعد از دیپلوی: ۶۰ثانیه صبر/ری‌تایم قبل از داوری |
| ۲۰ | ۰۹-۱۹ | تکرار باگ ۱۲ (مقایسهٔ ساب شامل خطوط uuid-صفر) | قانون ثبت‌شده اجرا نشد | فیلتر استاندارد خطوط واقعی = تابع مشترک؛ تکرار = نقص فرایند |
| ۲۱ | ۰۹-۱۹ | بازآرایی onData در ورکر تانل gb/jp را قطع کرد (de/us سالم!) | جابجایی ثبت listener پیام در workerd قابل‌اتکاست؛ علت دقیق isolate-level نیازمند لاگ CF | ساختار هندلر پیام ورکر = فقط با dispatchEvent-تزریق گسترش داده شود؛ هر بازآرایی = رگرسیون تانلِ همهٔ کشورها |
| ۲۲ | ۰۹-۱۹ | کلاینت خام WS دو دور بی‌نتیجه ماند | درخواست دستی 
 پایانی نداشت | قبل از شک به سیستم، تستر خودت را تست کن (درجه ۴ بند ۸) |
| ۲۳ | ۰۹-۱۹ | ورکر تازه‌دپلوی دو بار «مرده» خوانده شد | پخش workers.dev گاهی >۵۰ثانیه | قانون ثابت: پس از دیپلوی ۷۵ثانیه صبر، ۳ری‌تایم، بعد داوری |
| ۲۴ | ۰۹-۱۹ | ترمیم gb/jp با نام foxy-de-* ساخته شد | sync_nodes_file فیلد prefix را نمی‌ساخت | prefix = foxy-+id همیشه؛ ترمیم مهارشده برای اعتبارسنجی مسیر ترمیم |
| ۲۵ | ۰۹-۱۹ | selfheal با exit ۱ شکست خورد (ترمیم‌ها موفق!) | گام git قدیمی در selfheal.yml با commitهای API تضاد داشت (تکرار کلاس ۷) | بعد از فیکس کلاس‌دار، همهٔ نمونه‌ها همان روز پاک شود |
| ۲۶ | ۰۹-۱۹ | حلقهٔ هشدار-کاذب ژاپن (دو ترمیم پیاپی) | نگهبان در پنجرهٔ پخشِ پس از ترمیم ورکر نو را مرده دید | dispatch فقط با مرگ دو-دور-پیاپی (streak) |
| — | ۰۹-۱۹ | اسکن راز BBN: ۴ مثبت کاذب ثبت | شناسهٔ D1 و uuid نمایشی پل راز عملیاتی نیستند | پذیرفته‌شده — تشخیص راز واقعی = توکن‌محور، نه الگو-محور صرف |
| ۲۷ | ۰۹-۲۰ | زنجیرهٔ vless ورکر دو باگ: هدر slice(0,18) بایت cmd را می‌انداخت + c.write روی ساکِت CF وجود ندارد | هدر ناقص هر اتصال را خراب می‌کرد؛ CF استریمی است (getWriter/getReader) | هدر ۱۹بایتی + استریم-API؛ صحت با سرور vless محلی اثبات (تسترِ تستر)؛ ضمناً فرگمنت #EPODONIOS در uri گیست نام گره را آلوده می‌کرد — جداسازی |
| ۲۸ | ۰۹-۲۰ | سه خط TR مستقیم از دید مشتری مرده بود («هیچکدوم کار نمی‌کنن») | برچسب TR از جئو+کامل‌بودن پارامتر ساخته شد نه از خروجی واقعیِ دید مشتری — نقض «پرچم = خروجی واقعی» | قانون: ورود به ساب اصلی فقط با تأیید دید مشتری؛ کاندیدها اول در ساب تست (/test) — حذف فوری از اصلی و ساخت شکارچی خودکار tr-hunt |
| ۲۹ | ۰۹-۲۰ | BBN-E: تانل هلند ×۲ مرده (عضو یگانه amsterdam-443) | استخر تک‌عضوی + قانون «بدون تغییر = بدون دیپلوی» مانع تعویض خودکار شده بود | dispatch تازهٔ nl-hunt؛ NL تا سبزشدن ×۲ بیرون ساب می‌ماند؛ استخر تک‌عضوی = ریسک مستثنی برای قانون بدون-تغییر || ۳۰ | ۰۹-۲۰ | شکارچی ساعت ۰۵:۱۹ استخر اثبات‌شدهٔ CA را با ۴ عضو تازه جایگزین کرد → CA از ایران قطع؛ همزمان تک‌عضو DE (falkenstein) پوسید → DE قطع | refresh = جایگزینی کامل با کاندیدهای تأییدشده‌فقط-از-Actions (ویناژ Actions ≠ لبهٔ CF/ایران) + DE استخر تک‌عضوی = تک‌نقطه‌مرگ | **قانون استخر-مقدس در nl_hunt:** عضو زندهٔ اثبات‌شده حذف نمی‌شود؛ فقط xray-مرده‌ها تعویض؛ پس از هر دیپلوی E-پروب ×۲ و قرمز=برگشت خودکار؛ منبع حق = CF نه فایل ریپو؛ DE/US به شکار اضافه شد؛ کالبد فالکنشتاین مرده تأیید شد (۲۵ کاندید DE امروز همگی مرده — بازیابی با شکار خودکار) |
| ۳۱ | ۰۹-۲۰ | اسپیدتست ژاپن از مشتری ارور می‌داد | اعضای JP زنده ولی ضعیف/بی‌ثبات (۴۰۲–۱۱۱۹۸Kbps نوسان؛ تانل ~۵۰۰–۱۱۰۰) — «زنده» ≠ «سالم» | ۴ عضو نو ۲۰+Mbps پایدار (۱۴۹.۲۲.۸۷.x) جایگزین؛ قانون v2: عضو ضعیف (<۱۵۰۰Kbps دور دوم) هم قابل‌تعویض با کاندید قوی (≥۲۵۰۰×۲دور) |
| ۳۲ | ۰۹-۲۰ | BBN: دو نسل ورکر موازی! ساب/نگهبان/مشتری روی نسل legacy (-932015/…) و شکارچی/تعمیرات روی نسل نو (824428/…) — کل تعمیرها به نسخهٔ اشتباه می‌رفت | نام‌گذاری دودوقتونه از ترمیم‌های قبلی؛ هیچ گارد هم‌راستایی نبود | قانون: منبع حق = دامنه‌های داخل ساب (legacy)؛ شکارچی/نگهبان/تعمیر همگی به همان نسل قفل شدند (فلیپ ۵ کشور)؛ تکرار: هر تغییر نسل = ممنوع بدون به‌روزرسانی ساب |
| ۳۳ | ۰۹-۲۰ | نگهبان ۵/۵ سبز می‌داد در حالی که تانل DE/CA از مشتری مرده بود | /usage فقط زنده‌بودن پروسه را می‌سنجد؛ پوسیدن عضو نامرئی | اندپوینت /tcheck (اتصال SS واقعی به عضو از دل ورکر) + نگهبان دودویی: پروسه (dispatch) + تانل (فقط گزارش) — CA/JP/US/GB سبز، DE صادقانه قرمز || ۳۴ | ۰۹-۲۰ | BBN-DE: سه شکار تجمعی (~۵۰ کاندید DE از تلگرام/مخازن/کشوری) همگی از سندباکس و لبهٔ CF قرمز؛ ۳ سرور TLS-زنده (87.120.84.32 tls / 167.233.219.201 reality / 88.216.220.88 grpc) احرازِ پروتکل را از دیتاسنتر رد می‌کنند | الگوی TR تکرار شد: کانفیگ‌های رایگان DE ضد-دیتاسنتر/فقط-ایران‌دوست هستند + uuidهای چرخان | حکم: قاضی = گوشی مشتری. ۳ خط 🇩🇪 D1/D2/D3 به ساب تست رفت (tr_hunt دیگر خطوط Germany را پاک نمی‌کند)؛ استخر DE = ۲ plain-vless تازه (tcheck هنگ‌شان را صادقانه نشان می‌دهد)؛ tcheck شاخهٔ vless + تایم-سخت ۶.۵s (ضد-هنگ watch)؛ probe_member شاخهٔ vless — اگر گوشی یکی را سبز کرد → خط مستقیم به اصلی (معماری TR) || ۳۵ | ۰۹-۲۰ | شکار RU با همهٔ درجات: ۳ منبع (کشوری-نبود/تلگرام ۲۲۰→۹vless/SS ۳۶۱→۸) → پروب ×۲: **۱۷/۱۷ مرده از دیتاسنتر** | الگوی مشترک DE/TR/RU: کانفیگ‌های رایگان این مبدأها ضد-دیتاسنتر یا مرده — کانال رایگان برای RU هم خشکید | خط RU اضافه نشد (پرچم = خروجی واقعی)؛ شکارچی RU را از قبل می‌گردد؛ راه باقی‌مانده برای گره دائمی EU/RU = HF-Space با توکن رایگان کاربر (پلن‌تأییدشدهٔ رودتم) — ورود خط به اصلی فقط با تأیید گوشی || ۳۶ | ۰۹-۲۰ | HF گره اختصاصی: توکن کاربر زنده (Wboyx ✓) ولی ساخت Docker/Gradio Space → **402: پلن PRO الزامی شد** (سیاست نو HF) — مسیر HF-رایگان بسته | سیاست پلتفرم، نه خطای ما؛ Static Space بدون کامپوت است | توکن ذخیره (کی‑۲۰) و نگه‌داری؛ جایگزین پیشنهادی: Render رایگان (ریجن فرانکفورت = خروجی 🇩🇪 واقعی، WS+TLS، keepalive کارخانه‌ای مانع خواب) — نیازمند یک API-کی از کاربر؛ بعدش همه‌چیز خودکار || ۳۷ | ۰۹-۲۱ | «دسترسی به آپدیت ساب داری؟» — ساب تست هیچ‌وقت آپدیت‌های tr.txt را نشان نمی‌داد | دو باگ هم‌زمان: ①TEST_URI_URL با ?v=1 پین و ۳۰۰ثانیه کش شده بود ②توکنِ محلی (KEYS) با binding زنده یکی نبود — تست‌های من همیشه با توکنِ کپی می‌شد | قانون: هر fetchِ محتوای متغیر = bust داینامیک بی‌کش (v3.12 اعمال شد)؛ هر شناسه/راز قبل از استفاده از سرویس زنده خوانده شود، نه از کپی محلی؛ پس از هر تغییر، مسیر کاملِ مشتری با شناسهٔ زنده بازتست شود || ۳۸ | ۰۹-۲۱ | گرهٔ فرانکفورت از رانر ~۳۰Mbps، از گوشی همکار صفرِ مطلق | دامنهٔ onrender.com از اپراتور مشتری بلاک (SNI قابل‌دیدن)؛ سبزِ رانر فقط نیمی از قضیه است | ورکر پاس‌-through (foxy-de-relay) روی دامنهٔ اثبات‌شدهٔ workers.dev؛ خط ۲ ساب به رله سوئیچ؛ تانل از دل رله ×۲ سبز (فرانکفورت ۲۰-۲۷Mbps)؛ فاکسی ۸ → v1.2.0 [۹]+E9 |

## #39 — زنجیرهٔ تغییر-نام/حذف-خط: flagmap هر خواننده را خاموش نکن (2026-09-21)
- **رخداد:** تغییر نام رله به «Germany 🦊» + حذف خط DE-۱ از de.txt → نگهبان «de» را کاذب-مرده گزارش کرد (uuids از پرچم 🇩🇪 می‌آمد و به uuid رله می‌خورد که با uuid ورکر legacy برابر نبود)؛ همچنین sync_nodes_fileِ selfheal اگر اجرا می‌شد، نود de را به ورکر رله (بدون tcheck/pool) می‌بست و de2 گم می‌شد.
- **فیکس:** NODE_IDS بدون «de» (آلمان مشتری = de2 با S_DE2)؛ MAP نگهبان + 🇫🇮=fi؛ selfheal flagmap 🇩🇪→de2 (pool_file=None) + 🇫🇮=fi؛ active_srv[de]=de-vless-0 برگشت.
- **قانون:** هر تغییر برچسب/حذف خط → MAP/flagmap «همهٔ خوانندگان de.txt» (watch JS، selfheal، nl_hunt) همزمان چک و پچ شود؛ پرچم تنها کلید نیست — جفت (پرچم، میزبان) ملاک است.
- **پیشگیری:** نود نو = سه پچ همزمان: خط de.txt + MAP/flagmap هر سه ابزار + binding نگهبان؛ وریفای /run تا dead:[].
